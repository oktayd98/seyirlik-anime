<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css"
        integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA=="
        crossorigin="anonymous" />
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.3.1/dist/jquery.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.7/dist/semantic.min.css">
    <script src="https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.7/dist/semantic.min.js"></script>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/newcollection.css">
</head>


<body>
    <div class="container">
        {{>header}}
        <div class="content-wrapper">
            {{>sidemenu}}
            <form method="POST" action="/admin/newcollection" class="newcollection-form">
                <div class="flex-row">
                    <div class="flex-item w50">
                        <label for="name">Koleksiyon Adı</label>
                        <input type="text" name="name" id="name">
                    </div>
                </div>

                 <div class="flex-row">
                    <div class="flex-item dropdown"> 
                        <label for="animes">Animeler</label>
                        <select id="animes" name="animes" class="ui fluid search dropdown" multiple="">
                            {{#each animes}}
                                <option value={{this._id}}>{{this.name}}</option>
                            {{/each}}
                        </select> 
                    </div>
                </div>

                <div class="flex-row">
                    <div class="flex-item text-area">
                        <label for="description">Açıklama</label>
                        <textarea name="description" id="description" cols="80" rows="10"></textarea>
                    </div>
                </div>

               

                <div class="flex-row">                 
                    <div style="margin-left: auto" "flex-item float-right"><button type="submit" class="btn btn-logo">Kaydet</button></div>
                </div>
            </form>
        </div>
    </div>

    <script src="/js/dropdown.js"></script>
</body>

</html>